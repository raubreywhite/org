# Error if any file holds more code lines than the limit

Error if any file holds more code lines than the limit

## Usage

``` r
check_max_loc_per_file(paths, max_loc)
```

## Arguments

- paths:

  Character vector of folder paths to check.

- max_loc:

  The maximum number of code lines a single file may hold.
